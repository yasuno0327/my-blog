
const initialState = {
  title: '',
  body: '',
  thumbnail_url: '',
  user_id: 0
}

export default function article(state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}
